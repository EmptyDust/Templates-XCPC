# 板子设计章程

v2 代码层。讨论定稿 2026-09-16。

## 一句话

瑞士军刀，不是重型装甲车。零件，不是框架。像 Unix 工具那样可组合、可兼容；拒绝全家桶绑架和类型依赖传染。

## 病

旧板子的重不在行数，在焊死。

- **全家桶绑架**：几件能分开的事收进同一个身份，并规定调用顺序。树上一个 `Tree` 揽直径、中心、重心；`getCenter` 必须先 `dfs1`，顺手吐的 `diam` 注释自己写「仅在中心位于直径中点时正确」。
- **结构焊荷载**：剖分的 `dfn`/`top` 是结构，区间加是荷载。HLD 内嵌写死语义的 `Segt`（「在此更新下递函数」），换 BIT 要挖内脏。荷载真的只是私货、外面永远不该碰，才允许藏。
- **类型依赖传染**：抄 A 必须先让 B 的类型进你的值。杂项 `Zmod` 与多项式 `MInt` 两套 mint；Comb 若返回 `Z`，后面 `int` DP 接不上。二维 `Point<T>` 与 SMU `P` 同理。
- **私宇宙**：每个文件头私造一份 DSU / BIT / IO，印出来的板子并不用它。图论例题的 `FOR`、带修莫队的 `a2`，书页没有定义。

装甲车是必须整辆开上场。军刀是口袋里很多把能互接的小刀：`cat | grep | wc`，不是带文件系统的 grep。

## 划线

病不是 struct 太大、章太长、名字叫 `Tree`。树遍历可以繁杂。HLPP 近两百行是算法本身。这不是全部肢解：Dinic、PAM、剖分拓扑仍是一个 `struct`；拆的是焊在一起的几件事，不是把一件事拆成七个助手函数。

两句判据：

1. **单体**：是一件事，还是一屋子事焊在同一个身份上、还规定了顺序？
2. **依赖**：抄 A 时 B 凭什么必须在场？算法的值就是那种对象（多项式系数、双哈希的 `U`/`V`）→ 依赖是刀刃的材料。只为类型干净（Comb 绑 `Z`）→ 传染，和两套 mint 同一类病。

四个检验：是否自携全套基础设施；做几件事；两个板子能否同编译单元共存、第三个不改前两者内脏就能接上；纸上每个非标准符号能否找到出处。

划线在 **结构与荷载分离**、**数据 + 自由动词**（`diameter(c)`，`path(u, v, op)`），对 **收编 + 时序协议**（`tree.getCenter()` 必须先 dfs）。解耦不是改名。

## 章程

1. **零件。** 无状态 → 自由函数 + 普通 `vector`。有本质状态 → 值语义轻 `struct`，单一职责，可拷贝可重建。结构（邻接表、剖分拓扑）和荷载（点权、区间加）分开；外面要够得着结构。返回值按值（或 `pair`），不用指针出参打断 `f(g(x))`。
2. **兼容。** 两个板子靠普通数据衔接（`vector`、dfn 区间），不靠同一个基类。最小形状：数据进、数据出，加一个回调。板子边界不用 `std::function`。
3. **基底极瘦。** `contest.hpp` 只放 `using` 和常量，极少宏；印成书上单独一页。不要收成下一个全家桶。
4. **原语一份。** DSU / BIT / mint / Point / Flow 各一个。mint：`Zmod<P>`，上场 `using Z = Zmod<MOD>`；双模再 `using U = Zmod<MOD1>`。多项式和双哈希用类型。Comb 与普通计数用手写 `% mod`，不绑 mint。`Point<T>` 与 SMU 的 `P` 是两套点。
5. **泛型仅当荷载类型真不确定。** BIT 的荷载、Dinic 的容量、点的坐标——该模板。点编号、阶乘表、始终是 `int` 的计数——不要套参数。
6. **形。** 类型用 `struct` 全公开，不要 `class`、不要 `private`。别名一律 `using`，不要 `typedef`。声明即实现，不拆头文件。`long long` 只认 `i64`；不要 `ll` / `LL` / 裸 `long long`。`__int128` 用 `i128`，不要叫 `i64`。`u64`、`__builtin_*ll`、`stoll` 不是别名。`#define int long long` 是卡常手段，不是 `i64`。
7. **不要过度压行。** 禁止把多句、循环体、`if` 和后面的 `return` 挤在一行。`if (x > n) init(x);` 这种一行一事留着。
8. **过界按下面的表传参。**

书页是说明书：中文节名，用法，真会 WA 的 pitfall，然后代码。不留没有代码的路标节。

## 传参

每次调用都过边界。g++ 不会把按值大容器优化成引用。

| 场景 | 写法 |
|---|---|
| 只读容器 / string / 大 struct | `const T&` |
| 有意改自己的副本（sort / shuffle / resize） | 按值，加注释说明 |
| ≤16B（`int` / `pair` / 坐标；x86-64 SysV 走寄存器） | 按值 |
| 返回值 | 按值 |
| 回调 | `F&&`，胶水 `[&]` |
| `std::move` | 只在所有权交接处 |

4MB `vector` 按值传十万次约 8.2s，`const&` 约 0。这是唯一致命项。小对象两种写法同一档。左值按值是拷贝，不是 move。`const&` 只读借用，`T&` 可变借用；const 是契约。

## 目标形态

- 树：邻接表是结构。直径、中心、重心不必塞进同一个对象，也不必先 dfs 再取中心。不是禁止树上出现 `struct`。
- 剖分：拓扑仍是一份剖分。荷载（区间加、最值）不焊进去；`path(u, v, op)` 把路径拆成区间回调。外面接 BIT 还是线段树，剖分零改动。
