#import "../prelude.typ": *

= 博弈论

公平组合游戏的判定速查：巴什 / Nim 家族与 SG 函数各变体。每节给题面引语与判定结论。选型信号：单堆取完胜 → 巴什系；多堆独立 → Nim 系（异或）；取完者负 → Anti 系；能拆堆 / 有向图后继 → SG 函数；对不上号的模型，从 SG 的 mex 定义出发推。

== 巴什博弈

最基础的取石子：每次 $1 tilde.op M$ 颗，判胜负只看 $N$ 对 $M+1$ 取模。

#quote(block: true)[
有 $N$ 个石子，两名玩家轮流行动，按以下规则取石子：

规定：每人每次可以取走 $X (1 <= X <= M)$ 个石子，拿到最后一颗石子的一方获胜。

双方均采用最优策略，询问谁会获胜。
]

#quote(block: true)[
两名玩家轮流报数。

规定：第一个报数的人可以报 $X (1 <= X <= M)$，后报数的人需要比前者所报数大 $Y (1 <= Y <= M)$，率先报到 $N$ 的人获胜。

双方均采用最优策略，询问谁会获胜。
]

- $N = K dot (M+1)$（其中 $K in NN^+$），后手必胜（后手可以控制每一回合结束时双方恰好取走 $M+1$ 个，重复 $K$ 轮后即胜利）；
- $N = K dot (M+1)+R$（其中 $K in NN^+, 0 < R < M+1$），先手必胜（先手先取走 $R$ 个，之后控制每一回合结束时双方恰好取走 $M+1$ 个，重复 $K$ 轮后即胜利）。

第二问（轮流报数）与取石子同构，同样按 $(M+1)$ 周期分析：报到 $N$ 等价于取走最后一颗。

== 扩展巴什博弈

每次取 $[a, b]$ 颗，周期变为 $a+b$；余数落在不够再取一次的区间，则当前手无法行动。

#quote(block: true)[
有 $N$ 颗石子，两名玩家轮流行动，按以下规则取石子：

规定：每人每次可以取走 $X (a <= X <= b)$ 个石子，如果最后剩余物品的数量小于 $a$ 个，则不能再取，拿到最后一颗石子的一方获胜。

双方均采用最优策略，询问谁会获胜。
]

- $N = K dot (a+b)$ 时，后手必胜；
- $N = K dot (a+b)+R_1$（其中 $K in NN^+, 0 < R_1 < a$）时，后手必胜（这些数量不够再取一次，先手无法逆转局面）；
- $N = K dot (a+b)+R_2$（其中 $K in NN^+, a <= R_2 <= b$）时，先手必胜；
- $N = K dot (a+b)+R_3$（其中 $K in NN^+, b < R_3 < a+b$）时，先手必胜（这些数量不够再取一次，后手无法逆转局面）；

== Nim 博弈

多堆独立、不可跨堆；判胜负只算异或和，是后续各类变体的基准。

#quote(block: true)[
有 $N$ 堆石子，给出每一堆的石子数量，两名玩家轮流行动，按以下规则取石子：

规定：每人每次任选一堆，取走正整数颗石子，拿到最后一颗石子的一方获胜（注：几个特点是*不能跨堆*、*不能不拿*）。

双方均采用最优策略，询问谁会获胜。
]

记初始时各堆石子的数量 $(A_1, A_2, dots, A_n)$，定义尼姆和 $S u m_N = A_1 xor A_2 xor dots xor A_n$。

*当* $bold(S u m_N = 0)$ *时先手必败，反之先手必胜。*

== Nim 游戏具体取法

用来构造一步走到异或和为 $0$ 的局面。

先计算出尼姆和，再对每一堆石子计算 $A_i xor S u m_N$，记为 $X_i$。

若得到的值 $X_i < A_i$，$X_i$ 即为一个可行解，即*剩下* $bold(X_i)$ *颗石头，取走* $bold(A_i - X_i)$ *颗石头*（这里取小于号是因为至少要取走 $1$ 颗石子）。

== Moore’s Nim 游戏 (Nim-K 游戏)

一次最多改 $K$ 堆（每堆取正整数颗）。把每位上 $1$ 的个数对 $K+1$ 取模：存在某一位不是倍数则先手必胜，全部是倍数则先手必败。

#quote(block: true)[
有 $N$ 堆石子，给出每一堆的石子数量，两名玩家轮流行动，按以下规则取石子：

规定：每人每次任选不超过 $K$ 堆，对每堆都取走不同的正整数颗石子，拿到最后一颗石子的一方获胜。

双方均采用最优策略，询问谁会获胜。
]

把每一堆石子的石子数用二进制表示，定义 $O n e_i$ 为二进制第 $i$ 位上 $1$ 的个数。

*以下局面先手必胜：*

*存在某一位，* $bold(O n e_i)$ *不是* $bold(K+1)$ *的倍数。*（注意是"存在一位"，不是"每一位"——后者过严。）

== Anti-Nim 游戏 (反 Nim 游戏)

最后取者输。全堆大小均 $<= 1$ 时看堆数奇偶（即异或是否为 $0$）；否则与普通 Nim 同判据。

#quote(block: true)[
有 $N$ 堆石子，给出每一堆的石子数量，两名玩家轮流行动，按以下规则取石子：

规定：每人每次任选一堆，取走正整数颗石子，拿到最后一颗石子的一方*出局*。

双方均采用最优策略，询问谁会获胜。
]

- 所有堆的石头数量均不超过 $1$，且 $bold(S u m_N = 0)$（也可看作“且有偶数堆”）；
- 至少有一堆的石头数量大于 $1$，且 $bold(S u m_N != 0)$。

== 阶梯 - Nim 博弈

把石子移到下一级等价于从本级“消失”；偶数级的移动可被对手镜像，故只对奇数级做传统 Nim。

#quote(block: true)[
有 $N$ 级台阶，每一级台阶上均有一定数量的石子，给出每一级石子的数量，两名玩家轮流行动，按以下规则操作石子：

规定：每人每次任选一级台阶，拿走正整数颗石子放到下一级台阶中，已经拿到地面上的石子不能再拿，拿到最后一颗石子的一方获胜。

双方均采用最优策略，询问谁会获胜。
]

对奇数台阶做传统 Nim：当 $S u m_N = 0$ 时先手必败，否则先手必胜。

== SG 游戏 (有向图游戏)

#specline([取石子情形约 #O($sum x dot m$)（与后继数相关）])
有向图上的后继取 $upright("mex")$。下面板子是取石子：先读 $m$ 种可取数量 `a[1..m]`，再读 $n$ 堆石子，每堆求 SG 后异或。

#pitfall[`num[]` 记忆化须先置 $-1$（`Solve` 里的 `memset` 多测每组都要清），带着上一组的残值会错判。]

SG 定理适用于有限、无环、正常规则的公平组合游戏：无合法行动者负，终止状态的 SG 为 0，其余状态取后继 SG 的 mex。SG 为 0 表示必败，非 0 表示存在必胜策略。每回合只移动一个独立子游戏时，总 SG 为各分量的异或；不能把反常规则的终态任意改值后继续套用。

#include-code("code/博弈论/sg.cpp")

== Anti-SG 游戏 (反 SG 游戏)

无合法行动者胜。正常规则下的 SG 值不能决定任意游戏的反常胜负，前面的 Anti-Nim 判据只在 Nim 模型中使用。

反例：后继表 `0: {}，1: {0}，2: {0,1}，3: {2}` 的正常 SG 为 `[0,1,2,0]`，反常规则的胜负为 `[胜,负,胜,负]`。状态 0 和 3 的 SG 同为 0，反常胜负却不同。

有限 DAG 可直接做胜负 DP：终态胜，非终态当且仅当存在必败后继时胜。下面返回 0-index 图上每个状态的胜负；多分量每回合只动一份时，需要对组合状态建图，不能直接异或单份答案。递归深度等于最长路径，长链应改逆拓扑顺序。

<反常规则-dag>
```cpp
vector<bool> misereWin(const vector<vector<int>> &adj) {
    int n = adj.size();
    vector<int> color(n);
    vector<bool> win(n);
    auto dfs = [&](auto &&self, int u) -> void {
        assert(color[u] != 1);  // 要求 DAG
        if (color[u] == 2) return;
        color[u] = 1;
        win[u] = adj[u].empty();
        for (int v : adj[u]) {
            self(self, v);
            if (!win[v]) win[u] = true;
        }
        color[u] = 2;
    };
    for (int u = 0; u < n; ++u) dfs(dfs, u);
    return win;
}
```

== Lasker’s-Nim 游戏 (Multi-SG 游戏)

可拆堆的 Nim，每堆套下面公式求 SG 再异或，不必再搜。

#quote(block: true)[
有 $N$ 堆石子，给出每一堆的石子数量，两名玩家轮流行动，每人每次任选以下规定的一种操作石子：

- 任选一堆，取走正整数颗石子；
- 任选数量大于 $2$ 的一堆，分成两堆非空石子。

拿到最后一颗石子的一方获胜。双方均采用最优策略，询问谁会获胜。
]

*本题使用 SG 函数求解，SG 值定义为：*

$
  bold(S G(x) = cases(
    x - 1 & quad x mod 4 = 0,
    x & quad x mod 4 = 1,
    x & quad x mod 4 = 2,
    x + 1 & quad x mod 4 = 3,
  ))
$

== Every-SG 游戏

在有限 DAG 上，每回合对每个尚未终止的分量各走一步，已终止的分量保持不动；所有分量都终止时，当前手负。各分量独立，移动一个分量不改变其他分量的合法后继。

定义 step：终态为 0；存在偶数 step 后继时，取这些后继的最大 step 加 1；否则取全部后继的最小 step 加 1。因此单局必胜态的 step 为奇数，必败态为偶数。这是考虑双方策略的递推，不是最长路径。

组合局面先手必胜当且仅当#strong[所有分量 step 的最大值为奇数];。证明：最大值为奇数时，奇数分量各走到最大的偶数后继，非零偶数分量各走到最小的奇数后继，可使新最大值为偶数。最大值为非零偶数时，任何走法中，原最大分量都会变成不小于它减一的奇数，而所有偶数后继都更小，故新最大值必为奇数。

例如后继 `2: {0,1}，1: {0}，0: {}` 中 step[2] 为 1；两条确定链分别剩 1、2 步时，最大值为 2，先手必败。

<every-sg-步数>
```cpp
vector<int> everySG(const vector<vector<int>> &adj) {
    int n = adj.size();
    vector<int> color(n), step(n);
    auto dfs = [&](auto &&self, int u) -> void {
        assert(color[u] != 1);  // 要求 DAG；长链改用逆拓扑 DP
        if (color[u] == 2) return;
        color[u] = 1;
        int largestEven = -1, smallest = INT_MAX;
        for (int v : adj[u]) {
            self(self, v);
            smallest = min(smallest, step[v]);
            if (step[v] % 2 == 0) largestEven = max(largestEven, step[v]);
        }
        if (!adj[u].empty()) step[u] = (largestEven >= 0 ? largestEven : smallest) + 1;
        color[u] = 2;
    };
    for (int u = 0; u < n; ++u) dfs(dfs, u);
    return step;
}
```

两份 DAG DP 均为 $cal(O)(N+M)$。Every-SG 用法：

```cpp
int main() {
    int n, m, k;
    cin >> n >> m >> k;
    vector<vector<int>> adj(n);
    for (int i = 0; i < m; ++i) {
        int u, v;
        cin >> u >> v;
        adj[u].push_back(v);
    }
    auto step = everySG(adj);
    int longest = 0;
    while (k--) {
        int start;
        cin >> start;
        longest = max(longest, step[start]);
    }
    cout << (longest % 2 ? "First\n" : "Second\n");
}
```

== 威佐夫博弈

两堆，可从单堆取或两堆同时取同样多。冷局面 $(a_k, a_k+k)$，$a_k = floor.l k phi.alt floor.r$，$phi.alt = (1+sqrt(5))\/2$。板子判 $("min", "max")$ 是否落在冷局面：差为 $k$ 时检查 $floor.l k phi.alt floor.r$ 是否等于较小堆。`int` 到 $10^9$ 用 `double` 一般够；再大换下面注释里的高精度常数，或上 `__int128` / 高精度。

#quote(block: true)[
有两堆石子，给出每一堆的石子数量，两名玩家轮流行动，每人每次任选以下规定的一种操作石子：

- 任选一堆，取走正整数颗石子；
- 从两队中同时取走正整数颗石子。

拿到最后一颗石子的一方获胜。双方均采用最优策略，询问谁会获胜。
]

*以下局面先手必败：*

$bold(((1, 2), (3, 5), (4, 7), (6, 10), dots))$ 具体而言，每一对的第一个数为此前没出现过的最小整数，第二个数为第一个数加上 $bold((1, 2, 3, 4, dots))$。

更一般地，对于第 $bold(k)$ 对数，第一个数为 $bold("First"_k = lr(floor.l frac(k (1 + sqrt(5)), 2) floor.r))$，第二个数为 $bold("Second"_k = "First"_k + k)$。

其中，在两堆石子的数量均大于 $10^9$ 时，由于需要使用高精度计算，我们需要人为定义 $frac(1 + sqrt(5), 2)$ 的取值为 $"lorry" = 1.618033988749894848204586834$。

#include-code("code/博弈论/wythoff.cpp")

== 斐波那契博弈

先手不能一次取完，之后每次不超过对方上次的两倍。当且仅当 $N$ 为斐波那契数时先手必败。`Force()` 预处理到 $upright("fib")[86] approx 8.9 times 10^17$，覆盖 `i64`；须在 `Solve` 前调用一次。`n` 超过 `int` 时把读入也改成 `i64`。

#quote(block: true)[
有一堆石子，数量为 $N$，两名玩家轮流行动，按以下规则取石子：

先手第 1 次可以取任意多颗，但不能全部取完，此后每人取的石子数不能超过上个人的两倍，拿到最后一颗石子的一方获胜。

双方均采用最优策略，询问谁会获胜。
]

当且仅当 $N$ 为斐波那契数时先手必败。

#include-code("code/博弈论/fibonacci.cpp")

== 树上删边游戏

删一条边并丢掉不与根相连的那一侧。须先建树到邻接表 `ver`（本段未定义，1-index）。`dfs` 返回 $R(v) = upright("SG")(v)+1$（叶子无孩子返 $1$），故 `dfs==1` 当且仅当根 SG 为 $0$，先手必败。复杂度 #O($N$)。根不是 $1$ 时改调用入口。

#quote(block: true)[
给出一棵 $N$ 个节点的有根树，两名玩家轮流行动，按以下规则操作：

选择任意一棵子树并删除（即删去任意一条边，不与根相连的部分会同步被删去）；

删掉最后一棵子树的一方获胜（换句话说，删去根节点的一方失败）。双方均采用最优策略，询问谁会获胜。
]

结论：当根节点 SG 值非 $0$ 时先手必胜。（原文写“非 $1$”，与上面叶子 SG $= 0$ 的约定不一致；板子比较的是返回值 $R = upright("SG")+1$ 是否为 $1$。）

*相较于传统 SG 值的定义，本题的 SG 函数值定义为：*

- 叶子节点的 SG 值为 $bold(0)$。
- 非叶子节点的 SG 值为其所有孩子节点 SG 值 $bold(+ 1)$ 的异或和。

#include-code("code/博弈论/tree-edge-game.cpp")

== 无向图删边游戏 (Fusion Principle 定理)

先按奇偶环缩点，再套树上删边。

#quote(block: true)[
给出一张 $N$ 个节点的无向联通图，有一个点作为图的根，两名玩家轮流行动，按以下规则操作：

选择任意一条边删除，不与根相连的部分会同步被删去；

删掉最后一条边的一方获胜。双方均采用最优策略，询问谁会获胜。
]

- *对于奇环，我们将其缩成一个新点+一条新边；*
- *对于偶环，我们将其缩成一个新点；*
- *所有连接到原来环上的边全部与新点相连。*

此时，本模型转化为“树上删边游戏”。
