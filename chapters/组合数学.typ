#import "../prelude.typ": *

= 组合数学
<组合数学>
计数的公式库：组合数与 Lucas、常用恒等式、卡特兰/斯特林、十二种球盒模型、容斥、群论计数（Burnside / Pólya）、康托展开、格路计数与约瑟夫。选型信号：问方案数/排列数 → 先在这章找公式；"本质不同"（旋转/翻转同构）→ Pólya；带边界限制的路径 → 反射原理/卡特兰。

== 组合数
<组合数>
#specline([模质数：预处理 #O($N$) 后单查 #O($1$)], [大 $n , k$ 小质模 → Lucas])
$binom(n, k) = n ! \/ (k ! (n - k) !)$，选 $k$ 个。模质数：预处理阶乘逆后 $cal(O)(1)$，或 Lucas 把 $n , k$ 按模拆开。模任意：质因数分解或杨辉。非法下标当 $0$。

=== debug
<debug>
提供一组测试数据：$binom(132, 66) =$ 377’389’666’165’540’953’244’592’352’291’892’721’700，模数为 $998244353$ 时为 $241 prime 200 prime 029$；$10^9 + 7$ 时为 $598375978$。

=== 逆元+卢卡斯定理（质数取模）
<逆元卢卡斯定理质数取模>
$cal(O)(N)$ 预处理阶乘及逆元后 $cal(O)(1)$ 查询。模数必须为质数，写在开头的 `mod` 里（按题目改）；依赖基础算法章的 `mypow`。返回 `int`，手写 $% mod$——组合数是一张表加三次乘，不绑取模类（双模哈希、多项式才用 `Zmod`，见杂项章）。`comb(1 << 21)` 预处理上限按题目改；查询超出时 `fac` / `inv` 自动 `init` 扩表。`_inv[i]=(i!)^{-1}`，不是 $i^(- 1)$。

```cpp
const int mod = 998244353;  // 模数按题目改
struct Comb {
    int n = 0;  // 当前已预处理到的上限
    vector<int> _fac{1}, _inv{1};  // 0! = 1，(0!)^{-1} = 1；_inv[i] = (i!)^{-1}

    Comb(int m) {
        init(m);
    }
    void init(int m) {
        if (m <= n) return;  // 只扩展不缩小
        _fac.resize(m + 1);
        _inv.resize(m + 1);
        for (int i = n + 1; i <= m; i++) {
            _fac[i] = (i64)_fac[i - 1] * i % mod;
        }
        _inv[m] = mypow(_fac[m], mod - 2, mod);  // 费马求最大阶乘的逆，再往回推
        for (int i = m; i > n; i--) {
            _inv[i - 1] = (i64)_inv[i] * i % mod;
        }
        n = m;
    }
    int fac(int x) {
        if (x > n) init(x);
        return _fac[x];
    }
    int inv(int x) {  // 返回 (x!)^{-1}
        if (x > n) init(x);
        return _inv[x];
    }
    int C(int x, int y) { // C(x,y)，非法下标返回 0
        if (x < 0 || y < 0 || x < y) return 0;
        return (i64)fac(x) * inv(y) % mod * inv(x - y) % mod;
    }
    int P(int x, int y) { // 排列数 P(x,y)
        if (x < 0 || y < 0 || x < y) return 0;
        return (i64)fac(x) * inv(x - y) % mod;
    }
} comb(1 << 21);  // 预处理范围按题目改
```

=== 质因数分解
<质因数分解>
此法适用于：$1 < n , m , "MOD" < 10^7$ 的情况。数组按 $10^7$ 开，模数写在开头的 `MOD` 里（按题目改）。`c()` 内部每次都跑欧拉筛，多测应把筛提出来只做一次。

#include-code("code/组合数学/质因数分解.cpp")

=== 杨辉三角（精确计算）
<杨辉三角精确计算>
$60$ 以内 `i64` 可解，$130$ 以内 `__int128` 可解。递推 $cal(O)(n^2)$，不取模；要取模请改用上面的 `Comb`。

#include-code("code/组合数学/杨辉三角精确计算.cpp")

== 常用组合数公式
<常用组合数公式>
- $k \* C_n^k = n \* C_(n - 1)^(k - 1)$ ；
- $C_k^n \* C_m^k = C_m^n \* C_(m - n)^(m - k)$ ；
- $C_n^k + C_n^(k + 1) = C_(n + 1)^(k + 1)$ ；
- $sum_(i = 0)^n C_n^i = 2^n$ ；
- $sum_(k = 0)^n (- 1)^k \* C_n^k = 0$ 。
- 二项式反演：$f_n = sum_(i = 0)^n binom(n, i) g_i arrow.l.r.double g_n = sum_(i = 0)^n (- 1)^(n - i) binom(n, i) f_i$，$f_k = sum_(i = k)^n binom(i, k) g_i arrow.l.r.double g_k = sum_(i = k)^n (- 1)^(i - k) binom(i, k) f_i$ ；
- $sum_(i = 1)^n i binom(n, i) = n \* 2^(n - 1)$ ；
- $sum_(i = 1)^n i^2 binom(n, i) = n \* (n + 1) \* 2^(n - 2)$ ；
- $sum_(i = 1)^n 1 / i binom(n, i) = sum_(i = 1)^n frac(2^i - 1, i)$ ；
- $sum_(i = 0)^n binom(n, i)^2 = binom(2 n, n)$ ；
- 拉格朗日恒等式：$sum_(i < j) (a_i b_j - a_j b_i)^2 = (sum_i a_i^2) (sum_i b_i^2) - (sum_i a_i b_i)^2$ 。

== lucas 定理
<lucas-定理>
#specline([递归层数 #O($"log"_p n$)])
$n , m$ 很大而模数 $p$ 为不超过约 $10^5$ 的质数时用。`C(a,b,p)` 需自行预处理小于 $p$ 的组合数（上面 `Comb` 或杨辉均可）。

Lucas 定理内容如下：对于质数 $p$，有

$ binom(n, m) mod p = binom(⌊n \/ p⌋, ⌊m \/ p⌋) dot binom(n mod p, m mod p) mod p $

观察上述表达式，可知 $n mod p$ 和 $m mod p$ 一定是小于 $p$ 的数，可以直接求解，$binom(⌊n \/ p⌋, ⌊m \/ p⌋)$ 可以继续用 Lucas 定理求解。这也就要求 $p$ 的范围不能够太大，一般在 $10^5$ 左右。边界条件：当 $m = 0$ 的时候，返回 $1$。

时间复杂度为 $O (f (p) + g (n) "log" n)$，其中 $f (n)$ 为预处理组合数的复杂度，$g (n)$ 为单次求组合数的复杂度。

```cpp
i64 Lucas(i64 n, i64 m, i64 p) {  // 原来参数名 lm，函数体用 m，无法编译
  if (m == 0) return 1;  // C(n,0)=1；C 需预处理到 p
  return (C(n % p, m % p, p) * Lucas(n / p, m / p, p)) % p;
}
```

```python
def Lucas(n, m, p):
    if m == 0:
        return 1
    return (C(n % p, m % p, p) * Lucas(n // p, m // p, p)) % p
```

== 范德蒙德卷积公式
<范德蒙德卷积公式>
两堆分别选再合并，等于从并集里直接选；网格路径计数也常用。

在数量为 $n + m$ 的堆中选 $k$ 个元素，和分别在数量为 $n 、 m$ 的堆中选 $i 、 k - i$ 个元素的方案数是相同的，即$sum_(i = 0)^k binom(n, i) binom(m, k - i) = binom(n + m, k)$ ；

变体：

- $sum_(i = 0)^k C_(i + n)^i = C_(k + n + 1)^k$ ；
- $sum_(i = 0)^k C_n^i \* C_m^i = sum_(i = 0)^k C_n^i \* C_m^(m - i) = C_(n + m)^n$ 。

==== 推论 1 及证明
<推论-1-及证明>
$ sum_(i = - r)^s binom(n, r + i) binom(m, s - i) = binom(n + m, r + s) $

==== 推论 2 及证明
<推论-2-及证明>
$ sum_(i = 1)^n binom(n, i) binom(n, i - 1) = sum_(i = 0)^(n - 1) binom(n, i + 1) binom(n, i) = sum_(i = 0)^(n - 1) binom(n, n - 1 - i) binom(n, i) = binom(2 n, n - 1) $

==== 推论 3 及证明
<推论-3-及证明>
$ sum_(i = 0)^n binom(n, i)^2 = sum_(i = 0)^n binom(n, i) binom(n, n - i) = binom(2 n, n) $

==== 推论 4 及证明
<推论-4-及证明>
$ sum_(i = 0)^m binom(n, i) binom(m, i) = sum_(i = 0)^m binom(n, i) binom(m, m - i) = binom(n + m, m) $

其中 $binom(n + m, m)$ 是我们较为熟悉的网格图路径计数的方案数。所以我们可以考虑其组合意义的证明。

在一张网格图中，从 $(0 , 0)$ 走到 $(n , m)$ 共走 $n + m$ 步。规定 $(0 , 0)$ 位于网格图左上角，其中向下走了 $n$ 步，向右走了 $m$ 步，方案数为 $binom(n + m, m)$。

换个视角，我们将 $n + m$ 步拆成两部分走，先走 $n$ 步，再走 $m$ 步，那么 $n$ 步中若有 $i$ 步向右，则 $m$ 步中就有 $m - i$ 步向右，故得证。

== 卡特兰数
<卡特兰数>
是一类奇特的组合数，前几项为 $1 , 1 , 2 , 5 , 14 , 42 , 132 , 429 , 1430 , 4862$ 。如遇到以下问题，则直接套用即可。

- 【括号匹配问题】 $n$ 个左括号和 $n$ 个右括号组成的合法括号序列的数量，为 $C a t_n$ 。
- 【进出栈问题】 $1 , 2 , dots.h , n$ 经过一个栈，形成的合法出栈序列的数量，为 $C a t_n$ 。
- 【二叉树生成问题】 $n$ 个节点构成的不同二叉树的数量，为 $C a t_n$ 。
- 【路径数量问题】在平面直角坐标系上，每一步只能#strong[向上];或#strong[向右];走，从 $(0 , 0)$ 走到 $(n , n)$ ，并且除两个端点外不接触直线 $y = x$ 的路线数量，为 $2 C a t_(n - 1)$ 。

计算公式：$C a t_n = frac(C_(2 n)^n, n + 1)$ ，$C a t_n = frac(C a t_(n - 1) \* (4 n - 2), n + 1)$ 。

== 斯特林数
<斯特林数>
第一类 $[n , m]$：分成 $m$ 个非空轮换；第二类 ${ n , m }$：分成 $m$ 个非空无标号子集。轮换有圆排列，子集没有。递推都是「自己开新组 / 加入已有组」。

=== 第一类斯特林数
<第一类斯特林数>
$[n , m]$ 为 $n$ 个元素分成 $m$ 个非空轮换的方案数。

- #strong[递推公式] $[n , m] = [n - 1 , m - 1] + (n - 1) dot [n - 1 , m]$ 从组合意义上讲即自己作为一个新的轮换 或者自己插入到已有的人的左边
- #strong[递推边界] $[0 , 0] = 1$

=== 第二类斯特林数
<第二类斯特林数>
${ n , m }$ 为 $n$ 个元素分成 $m$ 个非空无序子集的方案数。

- #strong[递推公式] ${ n , m } = { n - 1 , m - 1 } + m dot { n - 1 , m }$ 从组合意义上讲即自己作为一个新的子集 或者自己插入已有的子集中
- #strong[常用公式] $x^n = sum_(k = 0)^n { n , k } (x)_k$ 常用化简 $n^m$
- #strong[递推边界] ${ 0 , 0 } = 1$
- #strong[计算公式] ${ n , m } = frac(1, m !) sum_(i = 0)^m (- 1)^(m - i) binom(m, i) i^n$

=== 普通幂 下降幂 上升幂互化
<普通幂-下降幂-上升幂互化>
$x^n = sum_(k = 0)^n { n , k } x^(underline(k))$ 普通幂转下降幂 $x^(underline(n)) = sum_(k = 0)^n [n , k] (- 1)^(n - k) x^k$ 下降幂转普通幂 $x^n = sum_(k = 0)^n { n , k } (- 1)^(n - k) x^(overline(k))$ 普通幂转上升幂 $x^(overline(n)) = sum_(k = 0)^n [n , k] x^k$ 上升幂转普通幂

=== 斯特林数恒等式
<斯特林数恒等式>
- $n ! = sum_(k = 0)^n [n , k]$

- $sum_(k = 1)^(n + 1) [n , k] { k , m } = binom(n, m)$

- $sum_(k = 1)^(n + 1) { n , k } [k , m] = binom(n, m)$

- ${ n , n - 1 } = binom(n, 2)$

- #strong[第二类斯特林数 行 即求出] ${ n , 0 } , { n , 1 } , dots.h , { n , n }$ 显然公式就是卷积形式 NTT 即可

- #strong[第二类斯特林数 列 即] ${ 0 , k } , { 1 , k } , dots.h , { n , k }$ 我们考虑设出其生成函数然后递推 根据递推式我们有 $H (n) = sum_i ({ i - 1 , k - 1 } + k { i - 1 , k }) x^i$ 那么有 $H (n) = frac(x H (n - 1), 1 - k x)$ 也即 $H (n) = frac(x^k, product_(i = 0)^k (1 - i x))$ 十分需要注意的是我们需要在分治以后把多项式的$"size"$重置为$k$ \(开小会 RE) 之后再求逆

- #strong[第二类斯特林数求和 即] $sum_(i = 0)^n sum_(j = 0)^i { i , j }$ 考虑按公式展开并交换求和次序 即 $sum_(j = 0)^n frac(1, j !) sum_(k = 0)^j (- 1)^(j - k) binom(j, k) sum_(i = 0)^n k^i$ 发现后面是个卷积形式 然后$O (n "log" n)$求得 $O (n)$遍历即可

== 莫茨金数
<莫茨金数>
不越过 $x$ 轴的 ${ 上 , 平 , 下 }$ 路径，或圆上不相交弦。

$M_n = M_(n - 1) + sum_(i = 0)^(n - 2) M_i M_(n - 2 - i) = frac((2 n + 1) M_(n - 1) + 3 (n - 1) M_(n - 2), n + 2)$ $M_n = sum_(k = 0)^(⌊ n \/ 2 ⌋) binom(n, 2 k) C_k$ \(C 为卡特兰数) 1, 1, 2, 4, 9, 21, 51, 127, 323, 835, …

==== #strong[经典应用场景:]
<经典应用场景>
1.一个圆上有 n 个点,画若干条不相交弦的方案数 2.在一个二维网格从$(0 , 0)$走到$(n , 0)$,$k_1$表示向右上走一步,$k_0$表示向右走一步,$k_(- 1)$表示向右下走一步 要求路径中始终不能低于 x 轴,终点必须在 $(n , 0)$ ，问方案数。

== 球盒模型
<球盒模型>
球是否相同、盒是否相同、盒是否可空，三种开关组出十二种。隔板法对应无标号球+可空盒；第二类斯特林对应有标号球+不可空无标号盒。对上条件再套公式。

=== $n$ 个球 全部放入 $m$ 个盒子
<n-个球-全部放入-m-个盒子>
==== I：球互不相同，盒子互不相同。
<i球互不相同盒子互不相同>
每个球都有 m 种选择，根据乘法原理，答案是$m^n$

==== II：球互不相同，盒子互不相同，每个盒子至多装一个球。
<ii球互不相同盒子互不相同每个盒子至多装一个球>
$n > m$ , 放不下，$0$ 种可能 $n < = m$ , \$ A\(m, n) \$

==== III：球互不相同，盒子互不相同，每个盒子至少装一个球。
<iii球互不相同盒子互不相同每个盒子至少装一个球>
容斥枚举空盒个数 $ sum_(i = 0)^m (- 1)^i \* C (m , i) \* (m - i)^n $

或 第二类斯特林数乘上 $m$ 的阶乘 $m ! dot "Stirling2" (n , m)$ ，答案为 $d p [n] [m] \* m !$ 。

==== IV：球互不相同，盒子全部相同。
<iv球互不相同盒子全部相同>
枚举几个盒子有球，第二类斯特林数，设 $f [k] [b]$ 为将 $k$ 个互异元素分为 $b$ 个不为空的集合 $ "ans" = sum \_ i = 0^m f [n] [i] $

==== V：球互不相同，盒子全部相同，每个盒子至多装一个球。
<v球互不相同盒子全部相同每个盒子至多装一个球>
$n > m , 0$ $n < = m , 1$

==== VI：球互不相同，盒子全部相同，每个盒子至少装一个球。
<vi球互不相同盒子全部相同每个盒子至少装一个球>
正是第二类斯特林数的定义，答案是 $f [n] [m]$ .

$ d p [n] [m] = {m \* d p [n - 1] [m] + d p [n - 1] [m - 1] & 1 lt.eq m < n\
1 & 0 lt.eq n = = m\
0 & m = = 0 且 1 lt.eq n $

==== VII：球全部相同，盒子互不相同。
<vii球全部相同盒子互不相同>
插板法， $C (n + m - 1 , m - 1)$ .

==== VIII：球全部相同，盒子互不相同，每个盒子至多装一个球。
<viii球全部相同盒子互不相同每个盒子至多装一个球>
选$n$个盒子放球， $C (m , n)$ .

==== IX：球全部相同，盒子互不相同，每个盒子至少装一个球。
<ix球全部相同盒子互不相同每个盒子至少装一个球>
先把每个盒子放一个球，然后转化为第 VII 个问题，插板法 $C (n - 1 , m - 1)$

==== X：球全部相同，盒子全部相同。
<x球全部相同盒子全部相同>
解 1：

问题等价于将 $n + m$ 拆分为 $m$ 个无序的正整数，根据 $"Ferrers"$ 图的理论， 等价于将 $n + m$ 拆分成若干个不超过 $m$ 的正整数，直接生成函数做。 $ T (n , m) = T (n , m - 1) + T (n - m , m) $ $ [x^(n + m - m)] product \_ i = 1^m frac(1, 1 - x^i) $

解 2：

$frac(1, (1 - x) (1 - x^2) dots.h (1 - x^m))$ 的 $x^n$ 项的系数。动态规划，答案为

$ d p [i] [j] = {d p [i] [j - 1] + d p [i - j] [j] & i gt.eq j\
d p [i] [j - 1] & i < j\
1 & j = = 1 med lr(||) med i lt.eq 1 $

==== XI：球全部相同，盒子全部相同，每个盒子至多装一个球。
<xi球全部相同盒子全部相同每个盒子至多装一个球>
同 V

==== XII：球全部相同，盒子全部相同，每个盒子至少装一个球。
<xii球全部相同盒子全部相同每个盒子至少装一个球>
解 1：

$ [x^(n - m)] product \_ i = 1^m frac(1, 1 - x^i) $

解 2：

$frac(x^m, (1 - x) (1 - x^2) dots.h (1 - x^m))$ 的 $x^n$ 项的系数。动态规划，答案为

$ d p [n] [m] = {d p [n - m] [m] & n gt.eq m\
0 & n < m\
 $

读入一组 $n , m$，依次输出 12 种模型答案，对应上文 I–XII，本质是对拍器。`solve4` 算出第二类斯特林行供 `solve6` 用，`solve10` 的生成函数系数留在 `ans[]` 给 `solve12` 用，顺序不能打乱。数组大小 $800010$、模数 $998244353$（NTT 模，原根 $3$）按题目改；$n$ 须能被补到 $2$ 的幂。

#include-code("code/组合数学/XII：球全部相同，盒子全部相同，每个盒子至少装一个球。.cpp")

== 容斥原理
<容斥原理>
下面例题用二进制或 dfs 枚举质因子子集。乘积可能爆 `i64`，板子里用 `t > n` / `s <= n / p[x]` 提前剪枝。

#quote(block: true)[
定义：$#scale(x: 120%, y: 120%)[\|] S_1 union S_2 union S_3 union dots.h union S_n #scale(x: 120%, y: 120%)[\|] = sum_(i = 1)^N lr(|S_i|) - sum_(i , j = 1)^N #scale(x: 120%, y: 120%)[\|] S_i inter S_j #scale(x: 120%, y: 120%)[\|] + sum_(i , j , k = 1)^N #scale(x: 120%, y: 120%)[\|] S_i inter S_j inter S_k #scale(x: 120%, y: 120%)[\|] - dots.h$
]

例题：给定一个整数 $n$ 和 $m$ 个不同的质数 $p_1 , p_2 , dots.c , p_m$，请你求出 1 ∼ $n$ 中能被 $p_1 , p_2 , dots.c , p_m$ 中的至少一个数整除的整数有多少个。

=== 二进制枚举解
<二进制枚举解>
$cal(O)(2^m dot m)$。子集积超过 $n$ 对答案无贡献，用 `t > n` 剪掉以免溢出。

#include-code("code/组合数学/二进制枚举解.cpp")

=== dfs 解
<dfs-解>
与二进制版等价，用 `s <= n / p[x]` 判断再乘是否溢出。入口 `odd=-1` 是因为空集先被跳过，选第一个质数时翻成 $+ 1$。

#include-code("code/组合数学/dfs-解.cpp")

== 群论计数
<群论计数>
染色计数，旋转/翻转视为相同。Burnside 数不动点，Pólya 按轮换个数 $m^(c (g))$。

=== Burnside 引理
<burnside-引理>
给定群 $G$ 在集合 $X$ 上的作用，则所有不同的轨道的数目

$ lr(|X \/ G|) = 1 / lr(|G|) sum_(g in G) lr(|X^g|) . $

这里，$X^g = { x in X : g x = x }$ 是 $g in G$ 的作用下的不动点集合。

=== Polya 引理
<polya-引理>
给定群 $G$ 在集合 $X$ 上的作用和颜色集合 $C$，则不同的染色方案的数目

$ lr(|C^X \/ G|) = 1 / lr(|G|) sum_(g in G) m^(c (g)) , $

这里，$m$ 是颜色数目，$c (g)$ 是元素 $g in G$ 的置换表示的轮换分解中的轮换数目。

=== 带权重形式的推广
<带权重形式的推广>
无权重版本的 Pólya 计数原理只能够给出所有的本质不同的染色问题的计数，但是在处理更为精细的问题时就无能为力了。比如说，如果在上述染色问题中，给定每种可以使用的颜色的数目，就不能套用上面的 Pólya 计数公式。在实际求解这类问题时，需要再次使用 Burnside 引理加以推导；而将这些结果总结为生成函数的形式，就是带权重版本的 Pólya 计数原理。

=== 例子
<例子>
==== 项链染色（带限制）
<项链染色带限制>
现在有一串共四个珠子的项链，每个珠子可以是红色或者蓝色，恰有两个红色珠子、两个蓝色珠子可以使用，计算共有几种本质不同的珠子。（如果两种染色的结果可以通过旋转项链重合，就认为是相同的。)

==== 解答和分析
<解答和分析>
考虑使用 Burnside 引理。红色、蓝色珠子各两个，共计有 $binom(4, 2) = 6$ 种染色方案。空间对称群 $G = { r_0 , r_1 , r_2 , r_3 }$ 分别对应旋转 $0 tilde.op 3$ 次，则它们对应的不动点集合分析如下：

- 旋转零次 $r_0 = (1)$，全部 $6$ 个染色方案都是不动点；
- 旋转一次 $r_1 = (1234)$，不动点要求所有珠子染同样的颜色，没有不动点；
- 旋转两次 $r_2 = (13) (24)$，有两个可独立染色的区域，大小都是 $2$，它们要分别染成红色和蓝色，则不动点集合的大小为 $2$；
- 旋转三次 $r_3 = (1432)$，与旋转一次的情形相同，没有不动点。

所以，根据 Burnside 引理，本质不同的染色数目为

$ frac(6 + 0 + 2 + 0, 4) = 2 . $

从这个例子中可以总结出如下计算方法。对于限制不同颜色个数的问题，同样是要把空间对称群中各个置换的轮换分别染色，但是需要让染色用到的颜色数目恰好等于给定的颜色个数。这样的组合问题通常没有显式解，除了可以通过排列组合方法计算的特殊情形外，需要看做背包问题进行求解。

通过生成函数可以给出这类计数问题的答案。给定置换 $g$，如果它的型是 $1^(alpha_1) 2^(alpha_2) dots.h.c n^(alpha_n)$，即它有 $alpha_k$ 个长度为 $k$ 的轮换，且对于每个轮换可以染成 $m$ 种颜色中的一种，那么生成函数

$ product_(k = 1)^n (sum_(i = 1)^m x_i^k)^(alpha_k) $

中单项式 $x_1^(beta_1) x_2^(beta_2) dots.h.c x_m^(beta_m)$ 的系数就是第 $i$ 种颜色用了 $beta_i$ 次的计数。这里圆括号中的表达式 $sum_(i = 1)^m x_i^k$ 的组合意义是，对于长度为 $k$ 的轮换，用到 $k$ 次颜色 $i$ 的染色方法的计数是 $1$，对于其它情形，计数是 $0$；这正描述了同一轮换中各位置染色一致的要求。

给定置换 $g$ 下染色计数的生成函数，对各个单项式应用 Burnside 引理，就得到各种颜色组合下的本质不同的计数。因为生成函数对各个单项式是线性的，所以本质不同染色方案的计数的生成函数是

$ 1 / lr(|G|) sum_(g in G) product_(k = 1)^n (sum_(i = 1)^m x_i^k)^(alpha_k) . $

展开这个式子，每个单项式的系数就给出了给定颜色组合下的本质不同染色的计数。

在上述过程中，对每个轮换进行染色的生成函数 $sum_(i = 1)^m x_i^k$ 并无特殊之处，可以替换成其它的生成函数。因而，有如下的一般版本的 Pólya 计数原理。

==== 置换群的轮换指标
<置换群的轮换指标>
给定置换群 $G$，则群 $G$ 的 #strong[轮换指标];（cycle index），定义为

$ Z_G (t_1 , t_2 , dots.h.c , t_n) = 1 / lr(|G|) sum_(g in G) t_1^(c_1 (g)) t_2^(c_2 (g)) dots.h.c t_n^(c_n (g)) , $

其中，$c_k (g)$ 是置换 $g$ 的轮换分解中长度为 $k$ 的轮换的个数，即 $1^(c_1 (g)) 2^(c_2 (g)) dots.h.c n^(c_n (g))$ 是置换 $g$ 的型。

==== Pólya 计数原理（带权重版本）
<pólya-计数原理带权重版本>
给定群 $G$ 在集合 $X$ 上的作用，对每个点的染色方法由它的染色方案的计数的生成函数 $f (x_1 , x_2 , dots.h.c , x_m)$ 给出，那么集合 $X$ 的本质不同染色方案的计数的生成函数是

$ Z_G (f (x_1^1 , x_2^1 , dots.h.c , x_m^1) , f (x_1^2 , x_2^2 , dots.h.c , x_m^2) , dots.h.c , f (x_1^n , x_2^n , dots.h.c , x_m^n)) , $

这里，$Z_G (t_1 , t_2 , dots.h.c , t_n)$ 是群 $G$ 的轮换指标。

这里，如果单个位置的染色的生成函数是 $f (x_1 , x_2 , dots.h.c , x_m)$，那么长度为 $k$ 的轮换的染色的生成函数就是 $f (x_1^k , x_2^k , dots.h.c , x_m^k)$。这反映了如果某一染色方案是给定置换的不动点，那么同一轮换中的所有位置必须染相同的颜色。如果将生成函数在 $x_i = 1$ 处取值，就得到上文的无权重版本的 Pólya 计数原理。

定理的叙述用到了置换群的轮换指标的概念。它和具体的染色问题无关。它描述了置换群的结构。

== 康拓展开
<康拓展开>
#specline([普通解 #O($n^2$)], [树状数组解 #O($n "log" n$)])
排列与其字典序排名互转。答案常取模。

=== 正向展开普通解法
<正向展开普通解法>
将一个字典序排列转换成序号。例如：12345-\>1，12354-\>2。`f[]` 用 `int`，只适于 $n lt.eq 12$；更大用下一节取模版。`kangtuo` 读全局 `str`。

#include-code("code/组合数学/正向展开普通解法.cpp")

=== 正向展开树状数组解
<正向展开树状数组解>
给定一个全排列，求出它是 1 \~ $n$ 所有全排列的第几个，答案对 $998244353$ 取模。

答案就是 $sum_(i = 1)^n "res"_(a_i) (n - i) !$ 。$"res"_x$ 表示剩下的比 $x$ 小的数字的数量，通过#strong[树状数组];处理（`BIT` 见数据结构章，不重抄）。复杂度 $cal(O)(n "log" n)$。`N`、`mod` 按题目改。

#include-code("code/组合数学/正向展开树状数组解.cpp")

=== 逆向还原
<逆向还原>
由 $1$-indexed 排名还原 $1 tilde.op n$ 的排列数字串。依赖上一节的 `jie_cheng` / `f[]`（须先调用）。`n` 须 $lt.eq 9$（用 `'0'+j` 拼串）；更大用 `vector<int>`。`f[]` 为 `int` 时 $n lt.eq 12$。

#include-code("code/组合数学/逆向还原.cpp")

== 基本格路计数问题
<基本格路计数问题>
#strong[问题描述:] 在平面上有多少从 $(0 , 0)$ 到 $(m , n) in N times N$ 点的格路经,其每一步都具有形式 $(1 , 0)$ 或 $(0 , 1)$\(即每一步沿水平方向向右走或沿铅直方向向上走一个单位距离)。

#strong[推导与结论:] 从 $(0 , 0)$ 到 $(m , n)$ 的路径,记沿水平方向向右走一个单位距离为 $E$,记沿竖直方向向上走一个单位距离为 $N$。其与多重集 ${ m dot E , n dot N }$ 的排列一一对应,一条路径对应该多重集上的一个全排列。 所以共有:

$ frac((m + n) !, m ! n !) = binom(m + n, m) $

种不同的走法。

== Dyck 路计数问题
<dyck-路计数问题>
不越过对角线的格路。$n , m$ 互质时用 Raney：循环移位里恰好一条是 Dyck 路。

格路模型是信息学竞赛中一种常见的问题模型,其中较为常见的一种是 #strong[Dyck 路计数];的相关问题。

=== 定义
<定义>
- #strong[格路 \(Lattice Path):] 在平面直角坐标系中,横坐标和纵坐标都是整数的点称为格点,平面格路是指从一个格点到另一格点只走格点的路,格路的#strong[长度];是指其所走的路的步数。
- #strong[自由路 \(Free Path):] 对于一条从 $(0 , 0)$ 到 $(m , n)$ 的格路,若其只使用了上步 $U = (0 , 1)$,水平步 $L = (1 , 0)$,则我们称其为 $(m , n)$ #strong[自由路];。
- #strong[自由路计数:] 记 $F (n , m)$ 为 $(n , m)$ 自由路的全集,$F (n , m) = \# F (n , m)$ 为 $(n , m)$ 自由路的总个数,即 $F (n , m)$ 的元素个数。可以发现,一条 $(0 , 0)$ 到 $(n , m)$ 的自由路可以唯一地对应到一个含有 $n$ 个水平步 $L$ 和 $m$ 个上步 $U$ 的序列。因此,$(n , m)$ 自由路的条数等于从 $n + m$ 个位置中选出 $n$ 个位置的方案数,即 $binom(n + m, n)$。 $ F (n , m) = binom(n + m, n) $
- #strong[\(n,m)-Dyck 路:] 对于一条从 $(0 , 0)$ 到 $(n , m)$ 的自由路,若其始终不经过对角线 $y = m / n x$ 下方,则我们称之为 $(n , m)$#strong[-Dyck 路];。记 $D (n , m)$ 为 $(n , m)$-Dyck 路的集合,$D (n , m) = \# D (n , m)$ 为 $(n , m)$-Dyck 路的数量。
- #strong[t-Dyck 路:] 特别地,若 $m = t dot n$,则我们称之为 $n$ 阶 #strong[t-Dyck 路];。

=== \(n,m)-Dyck 路的计数
<nm-dyck-路的计数>
#strong[定理 2.2 \(互质情况):] 当 $n , m$ 互质时,$(n , m)$-Dyck 路的数量为: $ D (n , m) = frac(1, n + m) binom(n + m, n) $ #strong[证明思路:] 利用 #strong[Raney 引理] 的推广或通过更复杂的双射构造。一个关键引理是,当 $n , m$ 互质时,对任意一个 $(n , m)$ 自由路,通过循环移位\(将其看作一个环),恰好只有一个循环同构的路径是 $(n , m)$-Dyck 路。总共有 $n + m$ 种循环移位,因此 Dyck 路占总数的 $frac(1, n + m)$。

=== 有 k 个峰的 \(n,m)-Dyck 路计数
<有-k-个峰的-nm-dyck-路计数>
- #strong[峰 \(peak):] 对于一条从 $(0 , 0)$ 到 $(n , m)$ 的自由路中的连续两步,若其为 $U L$,则我们称之为#strong[峰];。
- #strong[谷 \(valley):] 若其为 $L U$,则我们称之为一个#strong[谷];。

#strong[定理 2.3:] 记 $F (n , m ; k)$ 为所有有恰好 $k$ 个峰的 $(n , m)$ 自由路的集合。则: $ F (n , m ; k) = binom(n, k) binom(m, k) $ #strong[证明:] 将 $U L$ 之间的格点叫做峰点。一个有 $k$ 个峰的路径,其峰点坐标为 $(x_1 , y_1) , dots.h , (x_k , y_k)$,满足 $0 lt.eq x_1 < x_2 < dots.h < x_k lt.eq n - 1$ 和 $1 lt.eq y_1 < y_2 < dots.h < y_k lt.eq m$。选择 $k$ 个 $x$ 坐标和 $k$ 个 $y$ 坐标可以唯一确定一条有 $k$ 个峰的路径,反之亦然。因此方案数为 $binom(n, k) binom(m, k)$。

#strong[定理 2.4:] 记 $F^(U L) (n , m ; k)$ 为所有有恰好 $k$ 个峰,且首步为 $U$,末步为 $L$ 的 $(n , m)$ 自由路的集合。则: $ F^(U L) (n , m ; k) = binom(n - 1, k - 1) binom(m - 1, k - 1) $

#strong[定理 2.5:] 记 $D (n , m ; k)$ 为所有有恰好 $k$ 个峰的 $(n , m)$-Dyck 路的集合。当 $n , m$ 互质时: $ D (n , m ; k) = 1 / k binom(n - 1, k - 1) binom(m - 1, k - 1) $ #strong[证明思路:] 对于任意一条 $F^(U L) (n , m ; k)$ 中的路径,其 $k$ 个峰点可以循环映射到 $k - 1$ 条不同的不合法路径上。因此,恰好有 $1 / k$ 的路径是 Dyck 路。

#strong[定理 2.6 \(t-Dyck 路):] 有 $k$ 个峰的 $n$ 阶 $t$-Dyck 路的个数是: $ D (n , t n ; k) = 1 / n binom(n, k) binom(t n, k - 1) $ 将 $k$ 从 1 到 $n$ 求和,可以得到 #strong[$n$ 阶 $t$-Dyck 路的总数];: $ D (n , t n) = frac(1, t n + 1) binom(t n + n, n) $

#strong[定理 2.8:] 从 $(0 , 0)$ 到 $(n , m)$ 的有 $k$ 个峰的 $t$-Dyck 路\($m = t n$)的个数是: $ D_t (n , m ; k) = frac(m - t n + 1, n) binom(n, k) binom(m, k - 1) $ #strong[定理 2.9:] 从 $(0 , 0)$ 到 $(n , m)$ 的 $t$-Dyck 路的总个数是: $ D_t (n , m) = frac(m - t n + 1, n + m + 1) binom(n + m + 1, n) $

== 不相交格路问题
<不相交格路问题>
多条格路互不穿越的方案。常化成行列式（LGV）或卡特兰之差。下面是不交 Dyck / 自由路的计数公式。

=== n 阶不交 Dyck 路计数
<n-阶不交-dyck-路计数>
#strong[定义 3.1:] 从 $(0 , 0)$ 到 $(n , n)$ 的两条 Dyck 路 $P , Q$。若 $Q$ 始终不穿过 $P$ \(即 $Q$ 在 $P$ 的下方或与之重合),则称 $(P , Q)$ 是一对#strong[不交 Dyck 路];。

#strong[定理 3.1:] $n$ 阶不交 Dyck 路对数为（$C_k$ 为卡特兰数）: $ C_(n + 2) C_n - C_(n + 1)^2 $ #strong[证明思路:] 通过构造一个巧妙的双射 $theta$,将一对不交的 Dyck 路 $(P , Q)$ 映射到从 $(0 , 0)$ 到 $(n + 1 , n + 1)$ 的一条 Dyck 路和从 $(1 , 1)$ 到 $(n + 2 , n + 2)$ 的一条 Dyck 路。利用这个双射关系,结合卡特兰数的性质进行推导。

=== 不交自由路计数
<不交自由路计数>
#strong[定义 3.2:] 设 $P , Q$ 是两条自由路,如果 $Q$ 始终不穿越 $P$,则称 $(P , Q)$ 是从 $(0 , 0)$ 到 $(n , m)$ 的两条#strong[不相交自由路对];。$F_(n c) (n , m)$ 表示不相交自由路对的个数。

#strong[定理 3.3:] 从 $(0 , 0)$ 到 $(n , m)$ 的不接触自由路对\(除了起点和终点)的个数为: $ F_(n t) (n , m) = frac(1, n + m - 1) binom(n + m - 2, n - 1) binom(n + m - 2, n - 1) $ #strong[定理 3.4:] 从 $(0 , 0)$ 到 $(n , m)$ 的不交自由路对个数是: $ F_(n c) (n , m) = frac(1, n + m + 1) binom(n + m + 1, n) binom(n + m + 1, n) $ #strong[证明思路:] 类似于不交 Dyck 路,通过双射将问题转化。将两条不交的 $(n , m)$ 自由路 $(P , Q)$ 构造成一条从 $(0 , 0)$ 到 $(n + 1 , m + 1)$ 的不接触自由路对,从而建立数量关系。

== 类 Dyck 路计数问题 \(斜向行走)
<类-dyck-路计数问题-斜向行走>
从 $(0 , 0)$ 走到 $(a , b)$,规定每次只能从 $(x , y)$ 走到左下或者右下,方案数记为 $f (a , b)$ 。

- $f (a , b) = binom(a, frac(a + b, 2))$ ；
- 若路径和直线 $y = k , k in.not [0 , b]$ 不能有交点,则方案数为 $f (a , b) - f (a , 2 k - b)$ ；
- 若路径和两条直线 $y = k_1 , y = k_2 (k_1 < 0 lt.eq b < k_2)$ 不能有交点,方案数记为 $g (a , b , k_1 , k_2)$ ,可以使用 $cal(O)(N)$ 递归求解；
- 若路径必须碰到 $y = k_1$ 但是不能碰到 $y = k_2$ ,方案数记为 $h (a , b , k_1 , k_2)$,可以使用 $cal(O)(N)$ 递归求解\(递归过程中两条直线距离会越来越大)。

从 $(0 , 0)$ 走到 $(a , 0)$,规定每次只能走到左下或者右下,且必须有#strong[恰好一次];传送\(向下 $b$ 单位),且不能走到 $x$ 轴下方,方案数为 $binom(a + 1, frac(a - b, 2) + k + 1)$ 。TODO：原式含未定义的 $k$，无法复原，保留待核。

== 格路计数与经典分拆恒等式
<格路计数与经典分拆恒等式>
格路计数方法可以用于建立和证明关于基本超几何函数和整数分拆的恒等式。

=== 枚举函数与 q-升阶乘
<枚举函数与-q-升阶乘>
- #strong[q-升阶乘符号:] $(x ; q)_n = product_(k = 0)^(n - 1) (1 - x q^k)$

- #strong[引理 1:]

  #block[
  #set enum(numbering: "i)", start: 1)
  + 纵向步骤 $V$ 起始的格路枚举函数为 $product_(i = 1)^m (1 - t q^(i - 1))^(- 1)$。
  + 纵向步骤 $V$ 起始且终点高度为 $n$ 的格路枚举函数为 $t^n \/ (q ; q)_n$。
  + 具有不等水平矢量表示的纵向步骤 $V$ 起始的格路枚举函数为 $(- t q ; q)_oo$。
  ]

=== Gauss 二项式系数
<gauss-二项式系数>
#strong[定义:] $[""_k^n]_q = frac((q ; q)_n, (q ; q)_k (q ; q)_(n - k))$

#strong[命题 3:] 终点坐标为 $(m , n)$ 的格路枚举函数为 Gauss 二项式系数 $[""_n^(m + n)]_q$。

=== Durfee 矩与经典分拆恒等式
<durfee-矩与经典分拆恒等式>
通过将格路按其所包容的最大 $(k + r) times k$ #strong[Durfee 矩形];进行分类,可以推导出复杂的组合恒等式。

- #strong[定理 5:] $ (t q)_oo^(- 1) = sum_(k = 0)^m mat(delim: "[", m - r; k) t^k q^(k (k + r)) \/ (t q) \_ k + r $
- #strong[系理 6 \(Kummer 定理的有限形式):]
  #block[
  #set enum(numbering: "i)", start: 1)
  + $(t q)_oo^(- 1) = sum_(k = 0)^m mat(delim: "[", m; k) t^k q^k \/ (t q)_k$
  + $(t q)_oo^(- 1) = sum_(k = 0)^oo t^k q^(k (k + r)) \/ (q)_k (t q)_(k + r)$
  ]

通过对这些公式取极限或特定值,可以得到 #strong[Euler 五角数定理];,#strong[Cauchy 恒等式];,#strong[Jacobi 三重积恒等式];等著名结论。

#strong[Euler 五角数定理];的极限形式为: $ (q)_oo = sum_(k = - oo)^oo (- 1)^k q^(k (3 k + 1) \/ 2) $

=== 格路计数的常用结论
<格路计数的常用结论>
==== 反射原理 \(Reflection Principle)
<反射原理-reflection-principle>
反射原理是一种巧妙的组合计数技巧,用于处理带有#strong[边界限制];的格路计数问题。其核心思想如下:

+ #strong[转化问题:] 将直接求解“合法路径数”的难题,转化为求解“总路径数”减去“不合法路径数”。总路径数通常很容易计算。

+ #strong[建立一一对应:] 将#strong[每一条不合法的路径];\(即触碰或穿越了限制边界的路径),通过一次#strong[轴对称变换\(反射)];,#strong[唯一地];映射到另一条从#strong[新的起点];出发到#strong[原终点];的无限制路径。

+ #strong[计数替换:] 由于不合法路径与新起点路径之间存在一一对应关系,因此“不合法路径的数量”就等于“从新起点到原终点的路径总数”。后者是一个无限制的格路问题,可以直接用组合数公式计算。

简单来说,就是用#strong[反射起点的无限制路径数];来等价替换#strong[原起点的非法路径数];。

#emph[图示:一条从 A 到 B 的不合法路径\(红色),在第一个接触点 P 处,将 A 到 P 段关于直线 L 反射,得到一条从反射点 A’到 B 的路径\(绿色)。]

#line(length: 100%)

=== 模型一:标准格路模型
<模型一标准格路模型>
- #strong[移动方式:] 每步只能向右 $(+ 1 , 0)$ 或向上 $(0 , + 1)$。
- #strong[总路径数:] 从 $(a , b)$ 到 $(c , d)$ 的总路径数为 $binom((c - a) + (d - b), c - a)$。

#strong[定理 1.1:单条边界线\(不能触碰)]

- #strong[问题:] 从 $(a , b)$ 到 $(c , d)$,路径不能触碰直线 $L : y = x + k$。

- #strong[前提:] 起点和终点都在直线 $L$ 的同一侧\(通常是下方)。

- #strong[反射起点:] 将起点 $(a , b)$ 关于直线 $L : y = x + k$ 反射,得到新起点 $A prime (b - k , a + k)$。

- #strong[公式:] $ upright("合法路径数") = binom((c - a) + (d - b), c - a) - binom((c - (b - k)) + (d - (a + k)), c - (b - k)) $ 即: #strong[\(从原起点到终点的总路径数) - \(从反射起点到终点的总路径数)]

#strong[定理 1.2:卡特兰数 \(Catalan Number)]

- #strong[问题:] 从 $(0 , 0)$ 到 $(n , n)$,路径不能越过对角线 $y = x$\(即不能到达 $y = x$ 上方)。

- #strong[等价问题:] 路径不能触碰直线 $y = x + 1$。

- #strong[公式:] $ C_n = binom(2 n, n) - binom(2 n, n + 1) = frac(1, n + 1) binom(2 n, n) $

=== 模型二:斜向格路模型
<模型二斜向格路模型>
- #strong[移动方式:] 每步只能右上 $(+ 1 , + 1)$ 或右下 $(+ 1 , - 1)$。
- #strong[总路径数:] 从 $(0 , 0)$ 到 $(a , b)$ 的总路径数为 $binom(a, frac(a + b, 2))$。\(要求 $a + b$ 为偶数且 $a gt.eq lr(|b|)$)

#strong[定理 2.1:单条水平边界线\(不能触碰)]

- #strong[问题:] 从 $(a , b)$ 到 $(c , d)$,路径不能触碰水平直线 $L : y = k$。

- #strong[前提:] 起点和终点都在直线 $L$ 的同一侧。

- #strong[反射终点:] 将终点 $(c , d)$ 关于直线 $L : y = k$ 反射,得到新终点 $B prime (c , 2 k - d)$。

- #strong[公式:] $ upright("合法路径数") = binom(c - a, frac((c - a) + (d - b), 2)) - binom(c - a, frac((c - a) + (2 k - d - b), 2)) $ 即: #strong[\(从起点到原终点的总路径数) - \(从起点到反射终点的总路径数)]

#strong[说明:]

- 在这两个模型中,反射起点还是终点是等价的,选择计算更方便的一个即可。

#pitfall[#strong["不能越过"];和#strong["不能触碰"];有区别："不能越过 $y = x$" 意味着路径上的点 $(x_p , y_p)$ 始终满足 $y_p lt.eq x_p$，等价于"不能触碰 $y = x + 1$"——比赛中要仔细审题。]

== 约瑟夫问题
<约瑟夫问题>
#specline([线性 #O($N$) 任意 $k$], [#O($K "log" N$) $K$ 小], [#O($sqrt(N)$) 单次大询问])
$n$ 个人编号 $0 , 1 , 2 dots.h , n - 1$ ，每次数到 $k$ 出局，求最后剩下的人的编号。`repeat(i,a,b)` 是 i 从 a 到 b-1 的宏，可等价写成 `for (int i = a; i < b; ++i)`。

```cpp
int jos(int n,int k){
    int res=0;
    repeat(i,1,n+1)res=(res+k)%i;
    return res;  // res+1，如果编号从1开始
}
```

$cal(O)(K "log" N)$ ，适用于 $K$ 较小的情况。

#include-code("code/组合数学/约瑟夫问题.cpp")

$cal(O)(sqrt(N))$

#include-code("code/组合数学/约瑟夫问题-2.cpp")

== 杂项
<杂项>
+ 考虑不定方程 $X_1 + X_2 + dots.h + X_n = m , (X_i > 0)$ 的解的数量 也就是把$m$分成$n$堆的方案数,我们运用插空法为 $binom(m - 1, n - 1)$
+ 考虑不定方程 $X_1 + X_2 + dots.h + X_n = m , (X_i > = 0)$ 的解的数量 我们把所有的$X_i$变为$X_i + 1$那么套用结论 1,方案数为 $C (m + n - 1 , n - 1)$
+ 在$n$个红球中插入$m$个蓝球,空格大小不限,可以插入两边,方案数为 $C (n + m , m)$ 证明:考虑有$n + 1$个空格可以插入,那么问题转化为不定方程 $X_1 + dots.h + X_(n + 1) = m , (X_i gt.eq 0)$ 的解的个数
+ 在$n$个红球中插入$m$个蓝球,空格大小不限,不可插入两边,方案数为 $C (n + m - 2 , med n - 2)$ 证明:考虑有$n - 1$个空格可以插入,那么问题转化为不定方程 $X_1 + dots.h + X_(n - 1) = m , (X_i gt.eq 0)$ 的解的个数，即 $C (m + n - 2 , med n - 2)$（原来误写成 $C (n + m - 2 , m - 2)$）
+ 考虑在$[1 , n]$区间内选$k$个数,满足$k$个数都不相邻 问选法多少种 考虑到不能相邻的原因,我们不妨考虑把每个选的数字和它的下一个数字绑在一起,特别地最后一个数字不用捆绑,那么答案为$binom(n - k + 1, k)$
