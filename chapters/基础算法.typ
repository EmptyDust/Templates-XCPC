#import "../prelude.typ": *

= 基础算法
<基础算法>
#quote(block: true)[
本章代码中 `i64` / `LL` 为 `long long`,`ld` 为 `long double`,如无声明按此理解。
]

== 常用函数
<常用函数>
赛场常写的几件：二进制快速幂、整数平方根纠偏、先除后乘的 lcm、整数 $log_2$、向 $plus.minus oo$ 取整的除法。C++ 除法向零取整，负数区间二分必须用这里的 `floor`/`ceil`。

#include-code("code/基础算法/快速幂与常用函数.cpp")

#quote(block: true)[
上述 `mypow` 中 `n * n` 在 `n ≥ 2^31` 时溢出：做大模数快速幂时改用 `__int128` 或数论章的防爆模乘。
]

#include-code("code/基础算法/log2floor.cpp")

#include-code("code/基础算法/sign-取整除法.cpp")

== 最大公约数 `gcd`
<最大公约数-gcd>
$gcd (a , b) = gcd (b , a med mod med b)$，辗转到 $0$。$cal(O) (log (a + b))$。日常用 `std::gcd`；卡常用二进制版（先剥公因子 $2$，再减）。

=== 欧几里得算法
<欧几里得算法>
#strong[速度不如内置函数！] 以 $cal(O) (log (a + b))$ 的复杂度求解最大公约数。与内置函数 `__gcd` 功能基本相同（支持 $a , b lt.eq 0$ ）。

```cpp
inline int mygcd(int a, int b) {  // 手写欧几里得，与 std::gcd 等价
    return b ? mygcd(b, a % b) : a;
}
```

=== 位运算优化
<位运算优化>
Stein：提出公共的 $2$，奇数相减代替取模。#strong[略快于内置，卡常用。] `tz` 是后导零个数。

#include-code("code/基础算法/gcd-位运算.cpp")

== 整数域二分
<整数域二分>
在单调谓词上找分界。先写清 `check(x)` 为真时答案在哪一侧，再选「第一个真」还是「最后一个真」。中点用 `l+(r-l)/2`，避免 `l+r` 溢出。

=== 通用模板
<通用模板>
在#strong[单调的 check] 上找”第一个满足条件的下标”。`check(mid)` 为 true 表示 mid 满足，此时答案在 `[l, mid]`，否则在 `[mid + 1, r]`。区间为左闭右闭，初始边界按题目放宽。

#include-code("code/基础算法/整数二分-通用.cpp")

=== 旧版（无法处理负数情况）
<旧版无法处理负数情况>
#quote(block: true)[
该写法用 `(l + r) / 2` 中点，C++ 除法对负数#strong[向零取整];，负数区间会出现 `mid = l` 的死循环，故仅适用于非负区间；且 `l + r` 有溢出风险。
]

- 在递增序列 $a$ 中查找 $gt.eq x$ 数中最小的一个（即 $x$ 或 $x$ 的后继）

#include-code("code/基础算法/整数二分-旧版-下界.cpp")

- 在递增序列 $a$ 中查找 $lt.eq x$ 数中最大的一个（即 $x$ 或 $x$ 的前驱）

#include-code("code/基础算法/整数二分-旧版-上界.cpp")

=== 新版
<新版>
`while (l <= r)` + 记录答案的写法：边界与无解处理更直观，推荐日常使用。以下两段分别求”第一个满足 `judge` 的下标”（后继）与”最后一个满足的下标”（前驱），`judge` 单调。

- $x$ 或 $x$ 的后继

#include-code("code/基础算法/整数二分-新版-后继.cpp")

- $x$ 或 $x$ 的前驱

#include-code("code/基础算法/整数二分-新版-前驱.cpp")

== 整体二分
<整体二分>
#strong[离线];算法：把”对一组询问做二分答案”的 $log$ 层二分合并成一次递归，每次递归对所有询问用一次 `cal` 同时向左/右儿子划分。复杂度约为 $cal(O) ((n + q) log V dot.op T_(c a l))$。适用条件：

+ 询问的答案具有可二分性

+ #strong[修改对判定答案的贡献互相独立];，修改之间互不影响效果

+ 修改如果对判定答案有贡献，则贡献为一确定的与判定标准无关的值

+ 贡献满足交换律，结合律，具有可加性

+ 题目允许使用离线算法

  ——许昊然《浅谈数据结构题几个非经典解法》

#include-code("code/基础算法/整体二分.cpp")

== 实数域二分
<实数域二分>
目前主流的写法是限制二分次数：100 次后区间长度缩小 $2^(- 100)$ 倍，对 `double` 精度绰绰有余，且比 `while (r - l > eps)` 免去选取 eps 的麻烦。注意 `l, r` 初始必须在答案两侧（可放宽到 ±1e9）。

#include-code("code/基础算法/实数二分.cpp")

== 整数域三分
<整数域三分>
用于#strong[单峰函数];（先减后增）求极小值：`check(x) <= check(x + 1)` 时极值在 `x` 及其左侧，否则在右侧。凸函数上三分；非单峰函数不可用。

#include-code("code/基础算法/整数三分.cpp")

== 实数域三分
<实数域三分>
单峰函数上取两个三等分点，丢掉较差的那一侧。限制 $100$ 次，免选 eps。非单峰会错。

限制次数实现，同样要求单峰：

#include-code("code/基础算法/实数三分.cpp")
