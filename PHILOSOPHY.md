# 风铃的模板库 · 哲学

本文件是完整哲学：代码层的章程（`DESIGN.md`）之外，还包括怎么认识、怎么验证、怎么工作。

---

## 0. 一句话

做瑞士军刀，不做装甲车。零件，不是框架。像 Unix 工具：`cat | grep | wc`，不是带文件系统的 grep。可组合性、兼容性优先于单体的完整能力。复杂度只花在算法本质，不花在"自带世界"上。

## 1. 正确

正确是理念，不是验收线。它不在代码写完之后到场，而在每一个决定之前：类型叫什么名、依赖给谁、接口吐什么、哪句话印在纸上。每一步先问"什么是对的"，再问"能不能过"。

按"能过"选，得到碰巧能过的东西；按"对"选，得到没有侥幸的东西。行为对是运气，形状对才是正确。

这是从 Rust 带来的立场：borrow checker 不证明算法对，它让一类错误**写不出来**。测试找已犯的错，哲学让错误没有机会发生。C++ 板子里没有编译器送这套纪律，就靠形去逼近。哲学是承重墙，对拍是保险丝——没有人靠保险丝盖楼，也没有楼不要保险丝。

从 Rust 学的是立场，不是语法：不把生命周期焦虑、trait、borrow 纪律写进 C++ 板子；`const` 是契约，签名说真话就够了。

## 2. 病：自带世界

大小不是罪。HLPP 两百行是算法本身。病是焊死，五种形态：

- **全家桶**：几件能分开的事收进同一个身份，还规定调用时序（先 `dfs1` 再 `getCenter`）。能力焊在身份上，正确性焊在顺序上。
- **结构焊荷载**：dfn 是别人用得着的结构，"区间加"只是一种答案。答案焊进刀里，这把刀就只对一道题正确。
- **依赖传染**：抄 A 必须先让 B 的类型进你的值。
- **私造原语**：书里已有一把刀，下一份板子再私铸一把几乎一样的。拷贝各自演化，一处修复到不了另一处。
- **隐式环境**：纸上出现 `a2`、`FOR`、`INF`、`MOD`，定义不在纸上。没印出来的东西等于不存在。

同一个病在仓库尺度再现：同一份板子在书页、源码文件、编辑器片段三处各存一份，各自演化——一处修复到不了另外两处。

## 3. 划线，不肢解

判据是"一件事还是一屋子事"，不是"对象还是函数"。树遍历可以繁杂；Dinic、PAM、剖分拓扑该是一个 `struct` 就还是一个。军刀是很多把整刀，不是一堆刀片。

- **结构对外**：别人还用得着的（邻接表、dfn、`fa`）必须直接够得着；只有外部永远不该碰的才允许藏。`path(u, v, op)` 把区间交给外面——换 BIT 还是线段树，剖分零改动。
- **原语一份**：DSU、BIT、mint、Point、Flow，每种一把。抄 A 需要自带一整份 B、而 B 本来就是板子——这就是私造。
- **依赖正当性只有一句**：这个算法离了那个对象还存在吗。多项式系数、双哈希的两个模——值本身是模整数，依赖是刀刃的材料；Comb 是一张表加三次乘，绑类型是图干净。问题带来的依赖是材料，实现顺手带来的依赖是绑架。

"一件事"在边缘会失灵：带统计的并查集、接口不同的两份自动机，按字面都说不清。这时用更深的一问裁——**谁拥有数据、谁规定你怎么活**：`fa` 是结构、连通块点数是荷载，所以带统计是另一把刀；两份完整板接口不同，是两把刀，不是私造。

## 4. 形

形是哲学的表面，不是审美：

- `struct` 全公开——零件没有封装可守；不用 `class` / `private`。
- 别名一律 `using`，不要 `typedef`。
- `long long` 只认 `i64`；`__int128` 叫 `i128`；`u64` 是无符号 64 位别名；builtin 后缀 `ll`、`stoll` 属于函数名。
- 泛型只在荷载类型真不确定时用：BIT 的 `T`、Flow 的容量、`Point<T>`；顶点编号、Comb 不套 `T`。
- `std::function` 是类型擦除，把静态刀变成动态框架；回调用 `F&&`，递归用 `self`。
- 签名说真话：只读 `const&`；有意改副本（sort / shuffle）按值并注明；小对象按值（x86-64 SysV 上 ≤16B 整数结构体走寄存器，两个 `double` 走 `xmm`）；返回值按值，不出参。
- 一行一事的 `if (x > n) init(x);` 留单行；不把整段压进一行，也不把单行拆成三行。

## 5. 书是说明书

上场抄的，不是课，不是迁移日志，不是 C++ 教程。

- 节的形状：中文节名 → specline → 用法 → 真会 WA 的 pitfall → 代码。
- 纸上有出处：每个非标准符号、每个依赖，翻得到它从哪来；公共基底印成一页。
- 注释写给抄的人，用现在时："不能写 `==1`：端点会判不相交"。不写"原来写成…"——那是给改稿人的历史。
- 浅与深都不是删留的理由。有防错细节的浅要留（`accumulate` 的 `0LL`）；现场推不出的深可以留（带花树）。无细节的定义课文、没有机会用到的论文块才是赘肉——没有打印预算就先不动。
- 模运算默认手写 `% mod`；取模类只在值的身份就是模整数的地方出现（多项式、双哈希）。
- 抄的人不是傻子，是被时间追着跑的人。使用体验至上：省一次翻页是舒适，防一次抄错才是体验。

## 6. 认识与验证

- 阅读有命中率，验证才确定。单遍慢读到不了零；同一份文本每换一个镜头——diff、元数据、按印刷原文拼装编译、运行断言、引用核对、口吻、溢出、版面宽度——就有一批新命中。问题长在镜头的交界处。
- 声称覆盖之前，先核对自己的覆盖记录。"无已知问题"和"无问题"之间，隔着的就是单遍阅读。
- 断言要实跑：注释里的数要跑出来；印刷块按纸面原文拼起来，编得过、算得对。
- 修改本身产生新的待检面——每写一句话都是新的犯错机会。改完的东西，要按改之前的同样标准再审。
- 收敛看形状：命中从"缺一个接口"降到"注释复读"，就是方法到底了。剩下的交给常驻闸：导出与内部链接、孤儿章、逐文件语法、标题宽度、图片、残渣、片段与书稿的一致及回归——闸随新发现的缺口生长。

## 7. 工作方式

- 一类一笔：同类改动同一个 commit，不按章切片；不同类不混在一笔。
- 约定一旦统一，不中途换风格；要换整体换，不留下两段长相。
- 未推送可以重做（amend / rebase），已推送不改写。
- message 只写 diff 里真有的东西：不写过程叙事（"修正三处""上一轮""不在本次范围"），不写加入又移除的幻影，不解释为什么没做不在 diff 里的事。subject 单行且短；正文每行约 35 字（72 列）。作者时间与提交时间一致。
- 逐板过判据：不 grep 点名，不把例子当范式；从头按章序慢慢扫，不跳段。
- 工具只信 g++（gnu++20）；不为 lint 引入 clang。
- 重写先对拍再入库；对拍用书中真实零件拼装，不另造一份"测试版板子"。

## 8. 停止

书停在"不欠债"的状态：每一条挂账都列在明面上，没有一处靠"大概没问题"撑着。完成不是没有可做的事，是没有藏着的事。
